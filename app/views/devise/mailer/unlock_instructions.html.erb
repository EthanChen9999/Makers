<p>
  <%= t("respect.hello") %>
  <%= @resource.email %>
</p>
<%= t("devise.mailer.lock") %>
<%= t("devise.mailer.unlock") %>

<p><%= link_to(t("devise.mailer.unlock_account"), unlock_url(@resource, unlock_token: @token)) %></p>
